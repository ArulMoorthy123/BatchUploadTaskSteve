import { ChatFileUrlPipe } from './chat-file-url.pipe';

describe('ChatFileUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ChatFileUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
